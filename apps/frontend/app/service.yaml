apiVersion: v1
kind: Service
metadata:
  name: hybrid-cloud-frontend-api
spec:
  selector:
    app: hybrid-cloud-frontend-api
    version: v1.0
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
